{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:preview": "wrangler deploy --env preview",
    "deploy:prod": "wrangler deploy --env production",
    "dev": "wrangler dev",
    "dev:remote-bindings": "wrangler dev --remote",
    "dev:preview": "wrangler dev --env preview",
    "dev:prod": "wrangler dev --env production",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "generate": "drizzle-kit generate:sqlite",
    "migrate": "wrangler d1 migrations apply pledge4peace-db",
    "migrate:preview": "wrangler d1 migrations apply preview-pledge4peace-db --env preview",
    "migrate:prod": "wrangler d1 migrations apply pledge4peace-db --env production",
    "studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@getbrevo/brevo": "^2.2.0",
    "@hono/zod-validator": "^0.5.0",
    "@sanity/client": "^7.6.0",
    "@types/bcryptjs": "^3.0.0",
    "bcryptjs": "^3.0.2",
    "drizzle-orm": "^0.29.5",
    "hono": "^4.0.0",
    "resend": "^4.5.1",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20250525.0",
    "drizzle-kit": "^0.20.0",
    "typescript": "^5.5.2",
    "vitest": "~3.0.7",
    "wrangler": "^4.16.0"
  }
}
